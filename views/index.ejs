<%- include("templates/header", {titulo: "Contactanos"})%>
        <%
        let a = 5;
        let b = 10;
        %>
        <div class="alert alert-primary"><%= a + b %></div>
        <h2>Resultado de la suma: <%= a + b %></h2>
    <h1><%= nombre%></h1>
    <h2><%= grupo%></h2>

    <h2>Desayunos</h2>
    <ul>

        <%
        desayunos.forEach((desayuno) => { %>
            <li><%= desayuno %></li>    
        <% }) %>
    </ul>
    <table class="table table-striped">
        <tr>
            <th>Desayuno</th>
        </tr>
        <% desayunos.forEach((desayuno) => { %>
            <tr>
                <td><%= desayuno %></td>
            </tr>
        <% }) %>
    </table>
<%- include("templates/footer")%>